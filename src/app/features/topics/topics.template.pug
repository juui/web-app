h1
  | {{::topicsController.title}}
ul
  li(ng-repeat="subTopic in ::topicsController.subTopics")
    a(ui-sref-active="active", ui-sref="topics.functions({topic:topicsController.topic, subTopic:subTopic.key})")
      //| {{subTopic.label}}
      | {{topicsController.topic}} - {{subTopic.key}}
ui-view()